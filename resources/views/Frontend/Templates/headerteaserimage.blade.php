<div id="ce_headerteaserimage">
    @if($data->ce_header)
        <h2>
            {{ $data->ce_header }}
        </h2>
    @endif

    @if($data->ce_subheader)
        <h4>
            {{ $data->ce_subheader }}
        </h4>
    @endif
</div>
